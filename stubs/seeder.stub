<?php

namespace {{ namespace }};

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class {{ class }} extends Seeder
{
    public function run()
    {
        // delete old data before running the seeder, to prevent duplicate data db error
        DB::table('')->delete();
    }
}
